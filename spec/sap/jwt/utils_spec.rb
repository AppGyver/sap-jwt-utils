# frozen_string_literal: true

RSpec.describe Sap::Jwt::Utils do
  it "has a version number" do
    expect(Sap::Jwt::Utils::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
